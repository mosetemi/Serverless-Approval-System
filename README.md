# Serverless-Approval-System
Severless certification microservice that automates the approval and denial process for managerial requests. This leverages step functions, dynamoDB tables for noSQL database and memory, IAM roles for permissions, Lambda for triggers, and an API Gateway used to configure request routes.
